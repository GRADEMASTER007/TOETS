# Cloud Build configuration for deploying AfriTrade & UAE Portal to Google Cloud Run
steps:
  # 1. Build the Docker container image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/afritrade-uae-portal:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/afritrade-uae-portal:latest'
      - '.'

  # 2. Push container image to Google Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/afritrade-uae-portal:$COMMIT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/afritrade-uae-portal:latest'

  # 3. Deploy container image to Google Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'afritrade-uae-portal'
      - '--image'
      - 'gcr.io/$PROJECT_ID/afritrade-uae-portal:$COMMIT_SHA'
      - '--region'
      - 'europe-west2'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '3000'
      - '--memory'
      - '1Gi'
      - '--cpu'
      - '1'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'
      - '--set-env-vars'
      - 'NODE_ENV=production'

images:
  - 'gcr.io/$PROJECT_ID/afritrade-uae-portal:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/afritrade-uae-portal:latest'

options:
  logging: CLOUD_LOGGING_ONLY
